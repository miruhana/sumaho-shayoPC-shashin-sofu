# 要件定義書：スマホ内データ軽量化ローカルWebアプリ

## 0. 改訂履歴
| 版 | 日付 | 内容 |
|---|---|---|
| v0.1 | 2026-09-12 | 初版作成（ヒアリング内容をもとに草案作成） |
| v0.2 | 2026-09-12 | 要確認事項5点への回答を反映し、方針を確定 |
| v0.3 | 2026-09-12 | 保存先（クラウドストレージ案を検討→不採用）、フォールバック時の自動削除方式を確定 |
| v0.4 | 2026-09-22 | 圧縮目標をGmail添付上限（25MB/ファイル）から**社用PCの送受信容量上限（合計10MB）**に変更。複数枚を「合計」で上限内に収める配分方式に変更。GitHub（Pages）での一般公開を確定 |

---

## 1. 背景・目的

- 現状、iPhone内の写真・データを会社PCへ送る際にGmailを利用しているが、**ファイルサイズが大きく送信できないケースが多発**している。
- 本アプリは、スマホ上で写真・データを**安全に（外部サーバーに一切送信せず）**、**画質をできるだけ落とさずに**軽量化し、Gmailで問題なく送信できる状態にすることを目的とする。
- 開発はClaude Codeを用いて行う想定。

---

## 2. 想定利用シーン

1. iPhoneで撮影・保存した写真（複数枚のことが多い）を会社PCに送りたい。
2. たまにPDFなどのファイルも一緒に送りたい。
3. 社用PCが送受信できる容量（10MB）に収まらず送信エラーになる → 本アプリで軽量化してから送信し直す。

---

## 3. 対象ユーザー・利用環境

| 項目 | 内容 |
|---|---|
| 利用者 | 依頼者本人（個人利用） |
| 主な端末 | iPhone（iOS） |
| ブラウザ | Safari（iOS標準ブラウザ）を想定 |
| PC側 | 会社PC（Gmailで受信） |
| ネットワーク | スマホの通常回線／Wi-Fi |

---

## 4. スコープ（対象範囲）

### 4.1 対象データ
- **写真（メイン対象）**：JPEG、HEIC、PNG
- **PDF等（サブ対象・頻度は低め）**：軽量化できればよいが、写真ほどの精度は求めない

### 4.2 対象外（今回のスコープ外とする）
- 動画ファイルの圧縮
- Office系ファイル（Word/Excel/PowerPoint）の圧縮
- 複数人での共有・アカウント管理機能
- クラウド連携（Googleフォト等との自動同期）

> 上記はヒアリング回答に基づく初期スコープです。将来的に対象を広げる場合は別途要件を追加してください。

---

## 5. 機能要件

### 5.1 ファイル選択・取り込み
- iPhoneの「写真」アプリまたは「ファイル」アプリから、**複数ファイルを一括選択**できること。
- 選択したファイルのサムネイル・ファイル名・元のファイルサイズを一覧表示すること。

### 5.2 軽量化処理（写真）
- 画像を**リサイズ（縦横ピクセル数の縮小）**および**再エンコード（圧縮率調整）**により軽量化する。
- HEIC形式の写真は、処理可能な形式（JPEG）に変換してから圧縮する。
  - **HEIC→JPEG変換は、ファイルを選択した直後に自動かつ高速に行う**（ユーザーが別途「変換」ボタンを押す必要はなく、選択→即変換という流れにする）。
  - 複数枚まとめて選択した場合も、変換処理を並行して進め、全体の待ち時間を最小化する。
  - 変換中はプログレス表示（「〇/〇枚 変換中」等）を出し、処理状況が分かるようにする。
- **選んだ写真「全体の合計」が、社用PCの送受信容量上限（10MB）に収まるまで自動的に圧縮率を調整**する処理を行う（1ファイルごとの上限ではなく、複数枚まとめた合計サイズで判定する）。
  - メール送信時はBase64エンコードにより実サイズの約1.37倍に膨らむため、安全側に倒し、実際の作業目標は**合計約7MB以下**とする（10MB ÷ 1.37 ＋ 余裕）。
  - 枚数や元のファイルサイズに応じて、写真ごとの圧縮目標バイト数をウォーターフィリング方式で自動配分する（すでに小さい写真は無変換のまま活かし、余った予算を残りの写真に回す）。
  - 例：まず軽い圧縮を試し、配分された目標を超える場合は段階的に圧縮率・解像度を上げて再試行する。
- 処理前後の合計ファイルサイズ・画質（プレビュー画像）を比較表示し、ユーザーが確認できるようにする。

### 5.3 軽量化処理（PDF等）
- PDF内に含まれる画像を再圧縮することでファイルサイズを削減する（テキスト自体の圧縮は対象外）。
- 写真ほどの精緻な画質調整は不要。「サイズを縮められるだけ縮める」程度のシンプルな処理でよい。

### 5.4 一括処理
- 複数ファイルをまとめて選択し、**一括で軽量化処理**を実行できること。
- 処理結果（成功／失敗、圧縮後サイズ）をファイルごとに一覧表示すること。

### 5.5 出力・共有
- **基本方針：共有シート経由でGmailに直接渡す（ファイルをスマホ内に永続保存しない）**
  - 軽量化後、iOS標準の共有シート（「共有」ボタンから出るメニュー）を呼び出し、ユーザーがそこで「Gmail」を選択することで、軽量化済みファイルがそのままGmailの新規メール画面に添付された状態で開く。
  - この方式であれば、ファイルはスマホのストレージを消費せず一時的にアプリ間で受け渡されるのみとなるため、**ストレージ容量の制約にも、会社PC側の「メール添付ファイルしか保存できない」というセキュリティ制約にも対応できる。**
  - **クラウドストレージ（Google Drive／Dropbox）への保存案は検討したが不採用とする**：会社PC側のセキュリティポリシー上、クラウドストレージ経由でアップロードされたファイルを保存できないため、今回の目的には合致しないと判断した。

- **フォールバック：共有シート経由が technically 難しい場合のみ、一時的にファイルアプリへ保存**
  - 共有シートでの直接受け渡しがうまく機能しない場合（複数ファイル同時共有時の制約など）に限り、軽量化済みファイルをiPhone標準の「ファイル」アプリ内の専用フォルダ（例：「軽量化済み（一時）」）に一時保存する。
  - ユーザーはその後、Gmailアプリを開き、「ファイル」から軽量化済みファイルを手動で添付する。
  - **技術的な制約の補足**：Webアプリの仕組み上、Gmailアプリに渡した後に「実際にメールが送信されたかどうか」をアプリ側で検知することはできない。そのため「送信完了を検知して自動削除」は実現できない。
  - **代替の自動削除仕様（確定）**：**次回このアプリを起動したタイミングで、前回一時保存した軽量化済みファイルを自動的に削除する。** これにより、ユーザーが手動で削除しなくても、一時ファイルが際限なく溜まってストレージを圧迫することを防ぐ。
    - 実装イメージ：アプリ起動時に、専用フォルダ内の前回セッションのファイルをチェックし、存在すれば削除してから通常の画面を表示する。

### 5.6 エラー・例外処理
- 圧縮してもなお社用PCの受信上限（10MB）を超える可能性がある場合（例：非常に多数のファイルをまとめて選択した場合等）は、ユーザーにその旨を分かりやすく通知し、複数回に分けて送信するよう案内する。
- 対応していないファイル形式が選択された場合は、エラーメッセージを表示しスキップする。

---

## 6. 非機能要件

### 6.1 セキュリティ・プライバシー（最重要要件）
- **すべての処理はスマホのブラウザ内（クライアントサイド）で完結**し、写真・データを外部サーバーに送信しないこと。
- 外部の分析ツール（アクセス解析タグ等）や、画像処理のために外部APIを呼び出すことは行わない。
- インターネット接続がなくても動作する（完全ローカル処理）ことが望ましい。

### 6.2 パフォーマンス
- 一般的なスマホ写真（数MB〜10数MB程度）を、数十枚単位でも実用的な時間（目安：1枚あたり数秒以内）で処理できること。

### 6.3 動作環境
- 主にiPhone（iOS）のSafariで正常に動作すること。
- Android等、他端末での動作保証は必須要件としない（動けば望ましい程度）。

### 6.4 ユーザビリティ
- 専門知識がなくても直感的に「選ぶ→軽量化→共有」の3ステップで完結する、シンプルなUIとする。
- 圧縮の強さ（画質優先／サイズ優先）を必要に応じて調整できるオプションがあるとよい（デフォルトは「社用PC容量上限（合計10MB）に収める」設定）。

---

## 7. 前提条件・制約事項

- Claude Codeで開発する「ローカルWebアプリ」として、サーバーを持たない静的なWebアプリ（HTML/JS/CSSのみ）として構築することを基本方針とする。
- iPhone単体で完結させるか、PCで一度ビルドしてからiPhoneに配布するかは、後述の「要確認事項」で方式を決定する。

---

## 8. 確定事項（要確認事項への回答）

実装前の論点について、以下のとおり方針が確定しました。

1. **アプリの起動方法** → **確定：PWA形式**
   - iPhoneのホーム画面にアイコンを追加し、1タップで起動できる形にする。

2. **HEIC形式の扱い** → **確定：自動・高速変換**
   - 写真選択直後に、ユーザー操作なしでHEIC→JPEG変換を自動実行する（5.2参照）。
   - 変換速度を重視し、複数枚でも待ち時間を感じにくいUI（進捗表示・並行処理）とする。

3. **軽量化後の共有方法** → **確定：共有シート優先＋ファイルアプリ保存のフォールバック**
   - 基本は共有シートからGmailへ直接渡す。
   - 難しい場合は、iPhone標準の「ファイル」アプリに保存し、そこからGmailで手動添付する運用でよい（5.5参照）。

4. **圧縮後もサイズが収まらない場合の挙動** → **確定：案内表示に留める**
   - 無理に画質を大きく犠牲にして収めようとはせず、収まらない場合は**「複数メールに分けて送信してください」等のメッセージを表示する**に留める。
   - 画質の過度な劣化より、案内表示によるユーザー判断を優先する。

5. **PDFの軽量化の優先度** → **確定：初期リリースは対象外**
   - まずは写真（HEIC→JPEG変換＋圧縮）のコア機能を完成させることを優先する。
   - PDF軽量化は、写真機能の完成後に追加検討する。

6. **保存先・フォールバック時の扱い** → **確定：クラウドストレージは不採用、共有シート優先＋自動削除**
   - Google Drive／Dropboxへの保存は、会社PC側のセキュリティポリシー（クラウド経由ファイルは保存不可）により不採用。
   - 共有シート経由でGmailに直接渡す方式を基本とし、ファイルを永続保存しない。
   - フォールバックで一時保存が発生した場合は、**次回アプリ起動時に前回ファイルを自動削除**する（5.5参照）。

---

## 9. 配布方法（Claude Codeを使わない人向け）

- 本アプリはビルド不要の単一HTMLファイルのため、**GitHubリポジトリを公開（Public）にし、GitHub Pagesで配信**する方式を採用する。
  - リポジトリ内には写真データを一切含まない（すべての処理が端末内で完結する静的ツールのため、公開しても個人情報の漏えいリスクはない）。
  - 発行されたURLを共有するだけで、Claude Codeやアカウント登録なしに誰でもブラウザからそのまま利用できる。
  - スマホのSafari／ChromeでURLを開き「ホーム画面に追加」すれば、アイコンをタップして起動できるPWAとして使える（HTTPS配信のためPWA化の制約を受けない）。
- ソースコードもリポジトリ上でそのまま閲覧・フォーク・改変が可能なため、社内の他部署等への展開や引き継ぎもしやすい。

## 10. 今後の進め方

1. 本要件定義書（v0.4）を確定版とする
2. Claude Codeで、PWA形式の「写真一括軽量化（HEIC→JPEG自動変換＋社用PC上限10MB内への圧縮）＋共有」のコア機能から実装開始
3. 動作確認・実機（iPhone）検証
4. GitHubリポジトリを公開し、GitHub PagesでURLを発行して共有
5. 必要に応じてPDF軽量化など追加機能を実装

---

以上
