# 要件定義書:スマホ→社用PC写真送付アプリ

## 1. 目的
私用iPhoneで撮影した写真を、Gmail経由で社用PCへ送付する。
Gmailの添付容量制限に収まるよう、送信前に自動でJPEG圧縮・軽量化を行う。

## 2. 想定利用シーン
- iPhoneで撮影/選択した写真(合計最大10GB相当・枚数不定)を社用PC宛のGmailアドレスへ送りたい。
- 手作業でのリサイズ・変換・分割送信をなくし、iPhone操作だけで完結させたい。

## 3. 機能要件
1. **写真選択**
   - iPhone標準の写真アプリから複数枚選択できること。
2. **軽量化・変換**
   - 選択した写真をすべてJPEG形式に変換する。
   - 各写真1枚あたりのサイズが10MB以下になるよう圧縮・リサイズする。
3. **送信**
   - 軽量化後の写真をGmailで送信する。
   - Gmailの実質上限(1通あたり添付25MB程度)を超えないよう、必要に応じて複数通に自動分割する。
   - 宛先(社用PCで受信するGmailアドレス)は固定 or 選択式。
4. **完了確認**
   - 送信対象の総容量・枚数・分割送信数をユーザーに提示する(送信前 or 送信後)。

## 4. 非機能要件
- 操作はiPhone単体で完結すること(社用PC側に専用アプリは不要、Gmail受信のみ)。
- 画質劣化は許容するが、実用上判別可能な範囲に留める。
- 通信失敗時にリトライ、または失敗分の特定ができること。

## 5. 実行環境
- **クライアント**:iPhone(私用端末)
- **実装方式(検討中)**:
  - a) iOSショートカット(自動化アプリ)で画像圧縮+Gmail送信を組む
  - b) ネイティブ/簡易アプリ(Swift等)を新規開発
- **サーバー側**:なし(社用PCはGmail受信のみ、追加アプリ不要)

## 6. 制約・注意事項
- Gmailの添付ファイル上限は実質25MB/通のため、10MB以下に圧縮した写真でも複数枚まとめる場合は通数分割が必要。
- 私用端末から社用メールへの送信となるため、社内の情報取扱いルール・セキュリティポリシーに適合しているか要確認。
- Gmail送信にはOAuth認証(Gmail API)または端末の標準メール送信機能を利用する想定。

## 7. 未確定事項
- 実装方式(ショートカット/ネイティブアプリ)の最終決定
- 宛先アドレスの固定有無
- 圧縮アルゴリズム・解像度基準の具体値
- 送信失敗時のリトライ仕様
